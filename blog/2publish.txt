--new blogpost: jargon]matrix CIV
in matrix or 2D-grid traversal, 
easier to treat origin as [0,0] and end as [N,N].
less amgiously to use South/north notation, as "higher/up/right...." are overloaded words

--optimal BST problem to move to _hint

--publish my problem "investment/residential property selling"

--add to blogpost on serialize a binTree, 
how about this technique:
assign incremental id's to each node in an inorder walk. then run preorder walk to output each node id. No null node needed